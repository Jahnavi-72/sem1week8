import React from 'react';

function App() {
  return (
    <div className="min-h-screen bg-gray-100 flex justify-center items-center">
      {/* Bouncing Element */}
      <div className="w-24 h-24 bg-blue-500 rounded-full animate-bounce">
        {/* Content inside the box (like a logo or icon) */}
      </div>
    </div>
  );
}

export default App;
